const initialState = {
  1: {
    title: 'curry'
  },
  2: {
    title: 'niku jaga'
  },
  3: {
    title: 'jaga bata'
  },
};

const recipes = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default recipes;
